#include"main.h"

/**
 * _abs - get the absolute number of a given number
 * @n: number to be returned
 * Return: absoluted value
 */
int _abs(int n)
{

	if (n < 0)
	{
		return (-n);
	}
	else
		return (n);
}
