#include<stdio.h>
#include<stdlib.h>

/**
 * mul - multiplies two integers
 * @a: integer to multiply
 * @b: integer to multiply
 * Return: Always 0 (success)
 */

int mul(int a, int b)
{
	int result;

	result = (a * b);
	return (result);
}
