#include<stdio.h>
#include<stdlib.h>

/**
 * main - entry point
 *
 * Return: Always 0 (Succcess)
 */

int main(void)
{
	char str[] = "with proper grammar, but the outcome is a piece of art,";

	printf("%s\n", str);
	return (0);
}
