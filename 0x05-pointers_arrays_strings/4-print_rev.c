include"main.h"

/**
 * print_rev - reverses string
 * @s: string inputed
 * Return: void
 */

void print_rev(char *s)
{
	int counter = 0;

	while (str[counter] != '\0')
	{
		counter++;

	for (int c = counter - 1; c >= 0, i--)
	{
		_putchar(str[i]);
	}
	_putchar('\n');
}
